# 星茴智农 AI 比赛演示版部署说明

## 1. 必须先更换已经暴露的密钥

1. 登录阿里云百炼控制台。
2. 删除或禁用之前发送到聊天中的 API Key。
3. 新建一枚 API Key，不要通过聊天、Word、截图或代码传递。

## 2. 在 Netlify 保存新密钥

进入当前网站项目：

`Project configuration` → `Environment variables` → `Add a variable`

填写：

- Key：`DASHSCOPE_API_KEY`
- Value：新创建的百炼 API Key
- 作用范围：默认全部范围即可；如果套餐支持单独选择，至少包含 `Functions`

以下配置已经写入代码，不需要重复设置：

- 地域：华北2（北京）
- 工作空间：`ws-c1q6j7ixvmlvsv5l`
- 视觉模型：`qwen3-vl-flash`
- 低成本文本模型：`qwen-flash`
- 文本调用：仅在用户点击生成种植方案或干燥方案时调用一次；浏览器等待上限 12 秒、服务端等待上限 10 秒，超时或异常时保留原有规则结果

如需更换模型或调整服务端等待时间，可选设置 `BAILIAN_MODEL`、`BAILIAN_TEXT_MODEL` 和 `ADVICE_TIMEOUT_MS`；比赛演示建议保持默认值。

## 3. 发布方式

AI 版包含 Netlify Functions 和图片存储，不能只上传纯 `dist`。推荐将生成的“Netlify AI 部署包”整个上传到 Netlify；登录状态下 Netlify 会根据 `netlify.toml` 执行构建并发布 `dist`，同时部署服务端函数。

如果拖入完整部署包后 Netlify 没有执行构建，请改用 Git 关联部署或 Netlify CLI。不要把 API Key 写进部署包。

## 4. 发布后验证

1. 打开“病虫害识别”，页面状态应显示 `qwen3-vl-flash 已连接`。
2. 点击“一键体验识别”，结果中的识别引擎应包含 `qwen3-vl-flash · AI核验`。
3. 上传自己的 JPG、PNG 或 WebP 图片并勾选匿名保存，结果应显示“匿名鉴定记录已保存”。
4. 生成种植方案和干燥方案，规则结果应先出现，随后补充 `qwen-flash` AI 增强解读；断网或接口超时时应继续保留规则结果。
5. 临时删除环境变量后，内置识别样本应回退到规则结果；自定义图片应明确提示 AI 未配置，不得伪造结果。

## 5. 图片记录说明

- 用户可以取消“匿名保存”勾选。
- 内置样本不会重复保存。
- 图片和识别元数据保存在当前 Netlify 项目的 `recognition-uploads` Blob store。
- 当前页面不公开历史记录列表；后续可以增加按记录编号查询、删除和导出功能。
- 比赛公开演示前，建议在 Netlify 用量页面确认限额，并在百炼控制台设置预算提醒。

## 6. 说明书可采用的技术表述

> 系统已通过 Netlify Functions 服务端接口接入阿里云百炼 qwen3-vl-flash 多模态模型，用于八角病虫害图片的可见特征提取与限定类别辅助研判，并为规则引擎生成的种植、产量和干燥方案提供结构化解释。产量区间、总产量、温度、时间和含水率等关键数值仍由规则引擎计算；模型输出经过类别白名单、字段校验、低置信度提示和人工复核约束。用户明确同意后，系统可将压缩图片及鉴定元数据匿名保存，为后续历史记录和样本积累预留条件。

不得写成“自研视觉模型”，也不得在没有独立测试集的情况下对外宣称确定识别准确率。
